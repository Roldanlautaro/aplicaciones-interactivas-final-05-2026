// TODO: Implementar BooksPage
// - Usar useEffect para obtener todos los libros con GET /books al montar el componente
// - Guardar el resultado en estado local con useState
// - Mostrar por cada libro: isbn, title, author, genre, availableCopies/totalCopies, status
// - Incluir un formulario para crear un nuevo libro (POST /books)
//   campos: isbn, title, author, genre (select), totalCopies
// - Permitir actualizar el status de un libro (PATCH /books/:id)

export default function BooksPage() {
  return <div>BooksPage - TODO</div>
}
