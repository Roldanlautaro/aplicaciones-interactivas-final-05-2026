// TODO: Implementar MembersPage
// - Usar useEffect para obtener todos los socios con GET /members al montar el componente
// - Guardar el resultado en estado local con useState
// - Mostrar por cada socio: memberNumber, name, email, status
// - Incluir un formulario para crear un nuevo socio (POST /members)
//   campos: memberNumber, name, email
// - Permitir actualizar el status de un socio (PATCH /members/:id)

export default function MembersPage() {
  return <div>MembersPage - TODO</div>
}
