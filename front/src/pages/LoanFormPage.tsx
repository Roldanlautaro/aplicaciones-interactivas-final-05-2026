// TODO: Implementar LoanFormPage
// - Al montar, hacer fetch de GET /books y GET /members para poblar los selects
// - Renderizar un formulario con:
//     - select de libro (bookId)
//     - select de socio (memberId)
// - Al hacer submit, llamar a POST /loans con { bookId, memberId }
// - Manejar y mostrar los errores que devuelva la API
//   (ej: socio suspendido, sin ejemplares disponibles, límite de préstamos)
// - En caso de éxito, redirigir a /loans

export default function LoanFormPage() {
  return <div>LoanFormPage - TODO</div>
}
